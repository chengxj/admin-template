<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:util="http://www.springframework.org/schema/util"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
		http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util-4.0.xsd">

	<bean id="shiroFilter" class="org.apache.shiro.spring.web.ShiroFilterFactoryBean">
		<property name="securityManager" ref="securityManager" />
		<!-- The 'filters' property is not necessary since any declared javax.servlet.Filter 
			bean -->
		<!-- defined will be automatically acquired and available via its beanName 
			in chain -->
		<!-- definitions, but you can perform instance overrides or name aliases 
			here if you like: -->
		<property name="loginUrl" value="/auth/unlogin" />
		<property name="unauthorizedUrl" value="/auth/unauthorized" />
		<!-- <property name="filters"> <util:map> <entry key="authc" value-ref="myOncePerRequestFilter" 
			/> <entry key="authc" value-ref="myAdviceFilter" /> </util:map> </property> -->
		<property name="filterChainDefinitionMap"
			value="#{filterChainDefinitionsLoader.loadDefinitions()}" />
		<!-- <property name="filterChainDefinitions"> <value> /login.html=anon 
			/index.html=anon /app/**=anon /assets/**=anon /js/**=anon /sys/user/pagination=authc,rest[sys:user:pagination:read] 
			/sys/user/*=authc,rest[sys:user:*:read] /** = anon </value> </property> -->
	</bean>

	<bean id="filterChainDefinitionsLoader" class="com.edgar.core.shiro.FilterChainDefinitionsLoader">
		<property name="dataSource" ref="db1.dataSource" />
	</bean>

	<bean id="securityManager" class="org.apache.shiro.web.mgt.DefaultWebSecurityManager">
		<!-- Single realm app. If you have multiple realms, use the 'realms' property 
			instead. -->
		<property name="realm" ref="myRealm" />
	</bean>
	<bean id="lifecycleBeanPostProcessor" class="org.apache.shiro.spring.LifecycleBeanPostProcessor" />

	<bean id="myRealm" class="com.edgar.core.shiro.CustomSecurityRealm">
		<property name="credentialsMatcher" ref="passwordMatcher" />
	</bean>

	<bean id="passwordMatcher"
		class="org.apache.shiro.authc.credential.HashedCredentialsMatcher">
		<!-- <property name="cacheManager" ref="cacheManager" /> -->
		<property name="hashAlgorithmName" value="MD5" />
		<property name="hashIterations" value="1024" />
		<property name="storedCredentialsHexEncoded" value="true" />
	</bean>

	<bean class="com.edgar.core.shiro.RetryLimitService">
		<property name="maxRetryCount" value="10" />
	</bean>

</beans>
